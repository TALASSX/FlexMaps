node_modules/
.tmp/
.vscode/

# Keep private certificates out of the repo
certs/

# Ignore source; track builds and packaged visuals
src/
*.ts
*.tsx

# Track built distribution and packaged visual files (do not ignore `dist/` or `*.pbiviz`)
